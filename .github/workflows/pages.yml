name: Deploy plots to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish; queue at most one more.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      # The plot HTML is committed; CI only builds the browsable index.
      - name: Build index pages
        run: python tools/build_index.py

      - name: Disable Jekyll processing
        run: touch site/.nojekyll

      - uses: actions/configure-pages@v6

      # include-hidden-files is required from v4 on, otherwise .nojekyll
      # is silently dropped from the artifact.
      - uses: actions/upload-pages-artifact@v5
        with:
          path: site
          include-hidden-files: true

      - id: deployment
        uses: actions/deploy-pages@v5
